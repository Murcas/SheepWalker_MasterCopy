using UnityEngine;
using System.Collections;

public class ShootBullet : MonoBehaviour 
{
	/*

	GameObject woolBullet;
	float attackSpeed = 2f;
	float coolDown;

	void Awake () 
	{
		woolBullet = GameObject.FindGameObjectWithTag("RamzookaBullet");
	
	}
	

	void Update () 
	{
		if(Time.time >= coolDown) 
		{
			if(Input.GetKeyDown (KeyCode.F)) 
			{
				Fire();
				Debug.Log ("Shoot");
			}
		}
	}


	void Fire() 
	{
		GameObject woolBullet = Instantiate(woolBullet, transform.position, Quaternion.identity) as GameObject;

		woolBullet.AddForce (Vector3.up * 100f);
			
		cooldown = Time.time + attackSpeed;
	}


*/

}
